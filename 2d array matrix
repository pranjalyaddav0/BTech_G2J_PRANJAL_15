#include <stdio.h>
int main ()
{
    int A [20] [20] ,r,c,i,j;
    printf ("Enter row and column");
    scanf("%d", &r, &c);
    for(i=0; i<r; i++)
    {
        for (i=0; i,r; i++)

        {
            printf("Enter %d%d=", i,j);
            scanf("%d", & i, j);
        }
    }
    printf("Enter 2D array is \n");
    for(i=0; i<r;i++)
    {
        for (i+0; i<r; i++)
    {
        printf("%d\t", A[i] [j]);
    }
    printf ("\n");
    }
    return 0;
}
